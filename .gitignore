node_modules/
dist/
__pycache__/
*.pyc
.DS_Store
.parity.json
# Orakel-Schwarm-Referenz + Auftragsdatei (Migrations-Stufe 6, tools/spectrum-check.mjs):
# ~25 MB End-Populations-Genome, in ~45 min neu erzeugbar (npm run oracle-swarm) —
# gleiche Begruendung wie .parity.json: reproduzierbares Rechenergebnis, kein Quellcode.
.swarm-oracle.json
.swarm-oracle-job.json
scratch*.png
scratch*
_*.mjs

# Persönliche lokale Claude-Code-Einstellungen (nicht versionieren)
.claude/settings.local.json

# Aufloesungs-Cache von tools/build-catalog.mjs (Artefakt, kein Quelltext)
tools/.catalog-cache.json

# Ernte-Zustand (Schritt 1.1) + Merkmalsquellen-Cache/Verknuepfung (Schritt 1.1b) —
# resumierbare Zwischenstaende bzw. heruntergeladene Fremddaten, kein Quelltext.
tools/.harvest-state.json
tools/.traits-cache/
tools/.traits-linked.json

# Eltern-Cache von tools/wikidata-lineage.mjs (Schritt 1.3) — dieselbe Begruendung:
# reproduzierbares Abfrage-Ergebnis (14.495 Arten in ~4 min), kein Quelltext. Er ist
# zugleich die Quelle der Wahrheit fuer die Ketten, weil die laufende Ernte
# tools/.harvest-state.json aus ihrem eigenen Speicherabbild ueberschreibt.
tools/.lineage-cache.json
